interface:
  display_name: "ChatLab"
  short_description: "Repo-specific patterns and workflows for ChatLab"
  default_prompt: "Use the ChatLab repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true